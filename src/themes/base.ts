import { ThemeType } from "./themeType";

const theme: ThemeType = {
  name: "base",
  selectors: [":root"],
  theme: {
    colors: {
      primary: "#40BCD8",
    },
  },
};

module.exports = theme;
